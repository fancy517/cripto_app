import SearchTokenItem from "../types/SearchTokenItem";

const mockCryptoSearch: SearchTokenItem = {
  rank: 1,
  name: "Bitcoin",
  symbol: "BTC",
  slug: "bitcoin",
  names: ["Bitcoin", "bitcoin", "BTC"],
};

export default mockCryptoSearch;
